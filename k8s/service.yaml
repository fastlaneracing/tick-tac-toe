apiVersion: v1
kind: Service
metadata:
  name: swamp-puppy-park
  namespace: swamp-puppy-park
spec:
  type: LoadBalancer
  selector:
    app: swamp-puppy-park
  ports:
    - name: http
      port: 80
      targetPort: http
